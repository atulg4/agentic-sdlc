# Architecture Decision Records

Use one numbered Markdown file per material decision. Include context,
decision, alternatives, consequences, security impact, migration, rollback,
status, date, and owner approval. Existing ADRs are immutable history; replace
a decision with a new ADR that supersedes the old one.
